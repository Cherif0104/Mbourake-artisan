/**
 * Drapeaux de fonctionnalités.
 * Permet de désactiver temporairement des modules sans supprimer le code
 * (ex: système de crédits en attente du système de commission).
 */

/** Si false : masquer l'achat de crédits et ne pas bloquer les artisans sans solde. */
export const CREDITS_ENABLED = false;
